#include <stdio.h>

int main(void)
{
	struct string
	{
		char s[15]
	};
	
	struct string[8] =
	{ {'aa'}, { 'ab'}, { 'ac' }, { 'bc' }, { 'ba' } };
	
	if (dict[j].word[i] > dict[k].word[i])
	{
		dict[temp] = dict[k];
		dict[k] = dict[j];
		dict[j] = dict[temp];
	}
}